services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: base
    environment:
      NODE_ENV: development
    ports:
      - "5173:5173"
    command: [ "yarn", "dev", "--host", "0.0.0.0" ]