services:
  app:
    build:
      target: base
    environment:
      NODE_ENV: development
    volumes:
      - ./node_modules:/app/node_modules
    ports:
      - "5173:5173"
    command: [ "yarn", "dev", "--host", "0.0.0.0" ]